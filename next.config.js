/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'pixinvent.com',
      'source.unsplash.com',
      'placeimg.com',
      'via.placeholder.com',
    ],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
